<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/links') %>
      <title>Quiz Game</title>
  </head>
  <body>
    <%- include('./partials/nav') %>
      <div class="container text-center">
        <% if (!user) { %>
          <h1>Please log in to your account!</h1>
          <a class="btn btn-success" href="/login">
            <i class="bi bi-box-arrow-in-right"></i> Login
          </a>
        <% } else { %>
          <h1 class="text-center  text-secondary py-2">Quiz Game!</h1>
          <% if (selectedCategory) { %>
            <form action="/addquiz" method="post">
              <% randomNotes.forEach((note, index) => { %>
                <div class="row justify-content-center mt-3">
                  <div class="showleft p-0 px-2">
                    <div class="card bg-info-dark">
                      <div class="card-body p-0 px-3">
                        <p 
                          for="answer<%= index %>" 
                          class="text-light fst-italic fw-bold text-waring mb-1 p-0 m-0">
                          Write the answer
                        </p>
                        <input 
                          name="answer<%= index %>"
                          type="text" 
                          class="form-control mb-2" 
                          placeholder="your answer..."
                          onmouseenter="handleMouseEnter(event)"
                        >
                        <input 
                          type="hidden"
                          name="front<%= index %>" 
                          value="<%= note.front %>"
                        />
                        <input 
                          type="hidden"
                          name="back<%= index %>" 
                          value="<%= note.back %>"
                        />
                        <div class="d-flex justify-content-center">
                          <p class="text-info fw-bold capitalize-once"> <%= note.back %></p> 
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
              <input 
                type="hidden"
                name="categoryName" 
                value="<%= selectedCategory %>"
              />
              <button 
                type="submit" 
                class="btn bg-info text-dark m-2 mb-2"
                onmouseenter="handleMouseEnter(event)"
              > 
                <i class="bi bi-box-arrow-down"></i>
                Save Answers
              </button>
            </form>
          <% } %>
        <% } %>
      </div>
      <%- include('./partials/footer') %>
      
  </body>
</html>

